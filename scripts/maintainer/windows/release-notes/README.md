# Product release notes (this version only)

CI / `release-evopanel-public` require:

`PRODUCT-NOTES-x.y.z.md`

matching `evopanel/package.json` `version` (e.g. `PRODUCT-NOTES-1.0.0.md`).

Public source-available line starts at **1.0.0**. Keep only the current version note in this folder; remove or archive older notes outside the tree when cutting a new version. Copy the previous note as a template when starting the next release.