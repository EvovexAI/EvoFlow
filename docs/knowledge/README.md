# 运营知识库（文档真相源在 ContentOS）

**Markdown SSOT 不在本仓库**，而在同级 ContentOS：

`../ContentOS/docs/智能内容运营平台/知识库/`

EvoFlow Gateway 启动时仍会自动注册内置 **「运营知识库」** Vault，**读取**上述目录（可用环境变量 `EVOFLOW_OPS_KNOWLEDGE_ROOT` 覆盖路径）。请在 ContentOS 侧编辑文档；本目录仅作指针，勿再维护第二份。

详见 ContentOS 该目录下的 `README.md`。
