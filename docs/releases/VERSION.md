# 版本管理

## 当前版本

- **版本**：待从现有发布流程确认
- **状态**：开发基线
- **目标分支**：`develop`

## 版本规则

使用 `MAJOR.MINOR.PATCH`：

- `MAJOR`：不兼容的 API、数据或运行时行为变化。
- `MINOR`：向后兼容的新功能。
- `PATCH`：向后兼容的缺陷修复、文档或安全补丁。

预发布版本使用 `-alpha.N`、`-beta.N` 或 `-rc.N` 后缀。每次发布必须更新 `CHANGELOG.md`，并在合并到 `main` 前完成验收、风险评估和回滚说明。
