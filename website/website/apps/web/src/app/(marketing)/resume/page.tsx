import { ResumePage } from "@/components/resume-page";

export const metadata = {
  title: "Resume",
  description:
    "Document-style overview of EvoFlow: capabilities, roadmap, core modules, and open-source entry points.",
};

export default function Page() {
  return <ResumePage />;
}
